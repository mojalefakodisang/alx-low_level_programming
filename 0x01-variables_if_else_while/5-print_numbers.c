#include <stdio.h>
/**
 * main: prints single digit numbers of base 10
 * return - Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d \n", i);
	}
	return (0);
}
