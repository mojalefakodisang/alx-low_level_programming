#include "main.h"
#include <stdio.h>

/**
 * mul - the function multiplies variables a and b
 * @a: first variable
 * @b: second variable
 *
 * Return: Returns product of the two variables on success
 */
int mul(int a, int b)
{
	return (a * b);
}
